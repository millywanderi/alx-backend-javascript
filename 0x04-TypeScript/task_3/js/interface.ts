// Build an interface and a type
export type RowId = number;

// Interface RowElement that contains these 3 fields
export interface RowElement {
  firstName: string;
  lastName: string;
  age?: number;
}
